import  { useState } from 'react'

/*
interface TaskProps {

  id: Number;

}*/

/*
type ChangeTaskHandlerType = () => {};
interface TaskListProps {
  onChangeTask: ChangeTaskHandlerType
}
*/


/*
interface TasksProps {
  tasks: [
    {
      id: number,
      name: String,
      done: boolean
    }
  ];
}
*/

interface 

// const TaskList = ({tasks: TasksProps,onChangeTask,onDeleteTask}) => {
const TaskList = ({tasks,onChangeTask,onDeleteTask}) => {

  return (
    <div className="task-list">
      <ul>
        {
          tasks.map(task => {
              return (
                <li key={task.id}>
                  <label>
                    <input type="checkbox" checked="" />
                      Visit Kafka Museum
                    <button>Edit</button>
                    <button>Delete</button>
                  </label>
                </li>
              )
          })
        }

      </ul>




      <input className="txt" type="text" value={taskName} onChange={handleChange} />
    <button className="btn" onClick={handleClick}>Add Task</button>
    </div>
  )
}

export default TaskList
